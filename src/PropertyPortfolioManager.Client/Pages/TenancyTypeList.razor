@page "/tenancytype"

<SectionContent SectionName="top-bar">
    <div class="row">
        <div class="col">
            <div>Tenancy Types</div>
        </div>
        <div class="col">
            @if (ActiveOnly)
            {
                <a @onclick="(() => IncludeInactive(true))">Include Inactive</a>
            }
            else
            {
                <a @onclick="(() => IncludeInactive(false))">Hide Inactive</a>
            }
            <a href="tenancytypeedit" class="btn btn-success btn-sm float-end">Create New Tenancy Type</a>
        </div>
    </div>
</SectionContent>

@if (tenancytypes == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="page-content">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Type
                    </th>
                    @if (!ActiveOnly)
                    {
                        <th>
                            Active
                        </th>
                    }
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var tenancytype in tenancytypes)
                {
                    <tr>
                        <td>
                            @tenancytype.Type
                        </td>
                        @if (!ActiveOnly)
                        {
                            <td>
                                @(tenancytype.Active ? "Active" : "Inactive")
                            </td>
                        }
                        <td>
                            <a href="@($"tenancytypeedit/{tenancytype.Id}")" class="btn btn-outline-primary btn-sm">
                                Edit
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
