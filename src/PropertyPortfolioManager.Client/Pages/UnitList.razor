@page "/unit"

<SectionContent SectionName="top-bar">
    <div class="row">
        <div class="col">
            <div>Properties</div>
        </div>
        <div class="col">
            @if (ActiveOnly)
            {
                <a @onclick="(() => IncludeInactive(true))">Include Inactive</a>
            }
            else
            {
                <a @onclick="(() => IncludeInactive(false))">Hide Inactive</a>
            }
            <a href="unitedit" class="btn btn-success btn-sm float-end">Add Property</a>
        </div>
    </div>
</SectionContent>

@if (units == null)
{
    <p><em>Loading...</em></p>
}
else
{
        <div class="row">
            @foreach (var unit in units)
            {           
                <ErrorBoundary>
                    <ChildContent>
                        <PropertyCard Property="unit"></PropertyCard>
                    </ChildContent>
                    <ErrorContent>
                        <p class="errorUI">Invalid property!</p>
                    </ErrorContent>
                </ErrorBoundary>

            }
        </div>

}
